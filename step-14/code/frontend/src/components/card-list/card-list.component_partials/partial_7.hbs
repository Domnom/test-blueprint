                <div className="cardContainer" data-testid="cardList.cardContainer">
                    {/* Here we use curly brackets the same way we do with properties, only this time we are inserting an array */}
                    {cardComponents}
