export const generateString = (length: number = 0): string => {

    let generatedData = "";

    for (var i = 0; i < length; i++)
    {
        generatedData += "a";
    }

    return generatedData;

}